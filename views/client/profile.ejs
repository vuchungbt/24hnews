<%- include('../layout/header', { categories, user, title: 'Hồ sơ cá nhân' }) %>

<div class="container mt-5">
  <div class="row">
    <div class="col-md-4">
      <div class="glass p-4 mb-4">
        <div class="text-center mb-4">
          <img src="<%= user.avatar || '/images/default-avatar.png' %>" alt="<%= user.username %>" class="rounded-circle img-thumbnail" style="width: 150px; height: 150px; object-fit: cover;">
          <h4 class="mt-3"><%= user.username %></h4>
          <p class="text-muted"><%= user.email %></p>
          <span class="badge <%= user.role === 'admin' ? 'bg-danger' : 'bg-primary' %>">
            <%= user.role === 'admin' ? 'Quản trị viên' : 'Người dùng' %>
          </span>
        </div>
        
        <div class="list-group">
          <a href="#profile" class="list-